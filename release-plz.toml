# release-plz manages per-repo version bumps and GitHub releases.
# Synchronized versions span the workspace; dev harnesses are publish=false in
# their own Cargo.toml and are skipped automatically.
[workspace]
# One version for the whole constellation; bump together.
dependencies_update = true
git_release_enable = true
# release-plz computes publish order from the dependency graph.
