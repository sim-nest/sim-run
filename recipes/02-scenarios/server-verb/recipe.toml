id = "server-verb"
title = "Server verb from loaded library"
codec = "shell"
setup = "setup.sh"
purpose = "purpose.md"
order = 60
tags = ["cli", "server", "verb", "offline", "deterministic"]
requires = ["sim-run-core", "symbol:server"]
network = false
