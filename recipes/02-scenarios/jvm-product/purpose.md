Run the published `sim jvm` front door. The bootloader loads the JVM library and its
CLI adapter, admits embedded classfile bytes, and reports static, managed-object,
array, guest-exception, and exact Java-concat outcomes.
